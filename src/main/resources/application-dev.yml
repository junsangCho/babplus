server:
  port: 8082

spring:

  datasource:
    url: jdbc:h2:file:./babplus;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: admin
    password: "!fhqht90"
    hikari:
      connection-timeout: 5000
      validation-timeout: 5000
      minimum-idle: 5
      max-lifetime: 240000
      maximum-pool-size: 20

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    show-sql: true

  sql:
    init:
      mode: always